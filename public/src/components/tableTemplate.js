import React, {useState, useEffect} from 'react';

const TableTemplate = (props) => {
console.log("in table template");
	  return (
	  <h3> table template here </h3>
  )
	};

export default React.memo(TableTemplate);